import Image from "next/image";import Link from "next/link";import { notFound } from "next/navigation";import { ArrowLeft, Clock3, Flame, Zap } from "lucide-react";import { Logo } from "@/components/icons";import { getPublishedRecipe } from "@/lib/recipe-repository";
export const dynamic="force-dynamic";
export default async function RecipePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=await getPublishedRecipe(slug);if(!r)notFound();return <main className="detail"><header><Link href="/"><Logo/></Link><nav><Link href="/#recettes">Recettes</Link><Link href="/#programme">Collections</Link><Link href="/#manifeste">Manifeste</Link></nav></header><section className="detail-hero"><div className="detail-photo"><Image src={r.image} alt={r.title} fill priority sizes="50vw"/></div><div className="detail-copy"><Link href="/#recettes" className="back"><ArrowLeft size={14}/> Retour aux recettes</Link><h1>{r.title}</h1><p>{r.description}</p><div className="detail-meta"><span><Clock3 size={17}/>{r.prepMinutes} min</span><span><Zap size={17}/>{r.protein}g protéines</span><span><Flame size={17}/>{r.calories} kcal</span></div></div></section><section className="recipe-body"><div><span className="eyebrow">À PRÉPARER</span><h2>Les ingrédients</h2><ul className="ingredients">{r.ingredients.map(x=><li key={x}>{x}</li>)}</ul></div><div><span className="eyebrow">EN CUISINE</span><h2>La méthode</h2><ol className="steps">{r.steps.map(x=><li key={x}>{x}</li>)}</ol></div></section></main>}
