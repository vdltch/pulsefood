import Link from "next/link";import {Check,ArrowRight} from "lucide-react";import {SimpleHeader} from "@/components/simple-header";import {CommercialForm} from "@/components/commercial-form";import {consumerPlans,packs,professionalOffers} from "@/lib/offers";
export const metadata={title:"PULSE+ et offres — PULSE Food",description:"Programmes personnalisés, packs et solutions PULSE pour particuliers et professionnels."};
export default function OffersPage(){return <main><SimpleHeader/><section className="offers-hero"><span className="eyebrow">PULSE, À TON RYTHME</span><h1>Moins de charge mentale.<br/><em>Plus de goût.</em></h1><p>Choisis l’accompagnement qui transforme vraiment tes semaines.</p></section><section className="plans">{consumerPlans.map(plan=><article key={plan.id} className={plan.featured?"featured":""}><span>{plan.name}</span><h2>{plan.price}</h2><small>{plan.period}</small>{plan.annual&&<b>{plan.annual}</b>}<ul>{plan.features.map(feature=><li key={feature}><Check/>{feature}</li>)}</ul>{plan.id==="plus"?<CommercialForm offer="PULSE_PLUS" source="pricing"/>:<Link href="/#recettes">Commencer gratuitement <ArrowRight/></Link>}</article>)}</section><section className="pack-section"><span className="eyebrow">SANS ABONNEMENT</span><h2>Des programmes à garder.</h2><div className="pack-grid">{packs.map(pack=><article key={pack.slug}><small>{pack.price}</small><h3>{pack.title}</h3><p>{pack.description}</p><CommercialForm offer="PACK" source={pack.slug} button="Être averti du lancement"/></article>)}</div></section><section className="premium-letter"><div><span className="eyebrow">LE DIMANCHE, EN MIEUX</span><h2>Le menu premium dans ta boîte mail.</h2><p>Un menu complet, sa liste de courses et les substitutions utiles.</p></div><CommercialForm offer="PREMIUM_NEWSLETTER" source="offers" button="Rejoindre la newsletter premium"/></section><section className="pro-section"><span className="eyebrow">PULSE POUR LES PROS</span><h2>Fais grandir l’impact.</h2><div>{professionalOffers.map(item=><article key={item.id}><span>{item.eyebrow}</span><h3>{item.title}</h3><b>{item.price}</b><ul>{item.features.map(feature=><li key={feature}><Check/>{feature}</li>)}</ul><CommercialForm offer={item.offer} professional source={item.id}/></article>)}</div></section></main>}
