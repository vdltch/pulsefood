import type { MetadataRoute } from "next";import { getPublishedRecipes } from "@/lib/recipe-repository";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const recipes=await getPublishedRecipes();const base="https://pulsefood.fr";return[{url:base,lastModified:new Date(),changeFrequency:"weekly",priority:1},{url:`${base}/programme`,lastModified:new Date(),changeFrequency:"weekly",priority:.9},{url:`${base}/mentions-legales`,changeFrequency:"yearly",priority:.2},{url:`${base}/confidentialite`,changeFrequency:"yearly",priority:.2},...recipes.map(r=>({url:`${base}/recettes/${r.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.8}))]}
