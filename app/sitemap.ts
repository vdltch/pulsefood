import type {MetadataRoute} from "next";import {getPublishedRecipes} from "@/lib/recipe-repository";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const recipes=await getPublishedRecipes(),base="https://pulsefood.fr",now=new Date();return[{url:base,lastModified:now,changeFrequency:"weekly",priority:1},{url:`${base}/programme`,lastModified:now,changeFrequency:"weekly",priority:.9},{url:`${base}/offres`,lastModified:now,changeFrequency:"monthly",priority:.8},{url:`${base}/packs`,lastModified:now,changeFrequency:"monthly",priority:.7},{url:`${base}/professionnels`,lastModified:now,changeFrequency:"monthly",priority:.6},{url:`${base}/createurs`,lastModified:now,changeFrequency:"monthly",priority:.6},{url:`${base}/mentions-legales`,changeFrequency:"yearly",priority:.2},{url:`${base}/confidentialite`,changeFrequency:"yearly",priority:.2},...recipes.map(recipe=>({url:`${base}/recettes/${recipe.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.8}))]}
