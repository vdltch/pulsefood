import { NextResponse } from "next/server";import { z } from "zod";import { getPublishedRecipes } from "@/lib/recipe-repository";import { getSession } from "@/lib/auth";import { db } from "@/lib/db";import { recipeInputSchema } from "@/lib/recipe-schema";import { slugify } from "@/lib/slug";
export async function GET(){const data=await getPublishedRecipes();return NextResponse.json({data,total:data.length})}
export async function POST(req:Request){if(!await getSession())return NextResponse.json({error:"Non autorisé"},{status:401});try{const data=recipeInputSchema.parse(await req.json());const slug=slugify(data.title);const recipe=await db.recipe.create({data:{...data,slug,publishedAt:data.status==="PUBLISHED"?new Date():null,ingredients:{create:data.ingredients.map((label,position)=>({label,position}))},steps:{create:data.steps.map((body,position)=>({body,position}))}}});return NextResponse.json({data:recipe,message:"Recette créée"},{status:201})}catch(e){return NextResponse.json({error:"Données invalides",details:e instanceof z.ZodError?e.flatten():undefined},{status:400})}}
