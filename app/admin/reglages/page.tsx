import { Download,Plus,Trash2,Upload } from "lucide-react";import { AdminShell } from "@/components/admin-shell";import { createTaxonomy,deleteTaxonomy,importRecipes } from "@/app/admin/actions";import { requireAdmin } from "@/lib/auth";import { db } from "@/lib/db";
export default async function SettingsPage(){await requireAdmin();const [categories,tags]=await Promise.all([db.recipeCategory.findMany({orderBy:{name:"asc"}}),db.recipeTag.findMany({orderBy:{name:"asc"}})]);return <AdminShell><main className="admin-main"><div className="admin-top"><div><span className="eyebrow">RÉGLAGES ÉDITORIAUX</span><h1>Organiser PULSE.</h1></div></div><div className="settings-grid"><Taxonomy title="Catégories" kind="category" items={categories}/><Taxonomy title="Tags" kind="tag" items={tags}/></div><section className="form-card data-card"><span className="eyebrow">DONNÉES</span><h2>Importer ou exporter les recettes</h2><p>L’import crée toujours des brouillons afin de garder une validation humaine avant publication.</p><div className="data-actions"><a href="/api/admin/export?format=json"><Download/>Export JSON</a><a href="/api/admin/export?format=csv"><Download/>Export CSV</a><form action={importRecipes}><input type="file" name="file" accept=".json,.csv" required/><button><Upload/>Importer</button></form></div></section></main></AdminShell>}
function Taxonomy({title,kind,items}:{title:string;kind:"category"|"tag";items:{id:string;name:string}[]}){return <section className="form-card taxonomy"><span className="eyebrow">{title.toUpperCase()}</span><form action={createTaxonomy.bind(null,kind)}><input name="name" placeholder={`Nouvelle ${title.toLowerCase()}`} minLength={2} required/><button><Plus/></button></form><div>{items.map(item=><p key={item.id}><span>{item.name}</span><form action={deleteTaxonomy.bind(null,kind,item.id)}><button aria-label={`Supprimer ${item.name}`}><Trash2/></button></form></p>)}</div></section>}
