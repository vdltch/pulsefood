import Image from "next/image";
import { AdminShell } from "@/components/admin-shell";
import { requireAdmin } from "@/lib/auth";
import { db } from "@/lib/db";

export const dynamic="force-dynamic";
export default async function AnalyticsPage(){await requireAdmin();const since=new Date(Date.now()-30*86400000);const [events,recipes]=await Promise.all([db.viewEvent.findMany({where:{createdAt:{gte:since}},select:{recipeId:true,createdAt:true}}),db.recipe.findMany({select:{id:true,title:true,image:true}})]);const byRecipe=new Map<string,number>();const byDay=Array.from({length:14},(_,i)=>({date:new Date(Date.now()-(13-i)*86400000),views:0}));for(const event of events){if(event.recipeId)byRecipe.set(event.recipeId,(byRecipe.get(event.recipeId)||0)+1);const day=byDay.find(x=>x.date.toDateString()===event.createdAt.toDateString());if(day)day.views++;}const ranking=recipes.map(r=>({...r,views:byRecipe.get(r.id)||0})).sort((a,b)=>b.views-a.views);const max=Math.max(...byDay.map(x=>x.views),1);return <AdminShell><main className="admin-main"><div className="admin-top"><div><span className="eyebrow">ANALYSES</span><h1>Ce qui donne faim.</h1></div><div className="analytics-total"><b>{events.length.toLocaleString("fr-FR")}</b><span>vues sur 30 jours</span></div></div><section className="analytics-chart"><div className="table-title"><h2>Tendance — 14 jours</h2></div><div className="bars">{byDay.map(day=><div key={day.date.toISOString()}><i style={{height:`${Math.max((day.views/max)*100,3)}%`}}><span>{day.views}</span></i><small>{day.date.toLocaleDateString("fr-FR",{day:"2-digit",month:"2-digit"})}</small></div>)}</div></section><section className="admin-table analytics-ranking"><div className="table-title"><h2>Vues par recette</h2><span>30 derniers jours</span></div>{ranking.map((recipe,index)=><div className="analytics-row" key={recipe.id}><b>{String(index+1).padStart(2,"0")}</b><div className="thumb"><Image src={recipe.image} alt="" fill sizes="46px"/></div><span>{recipe.title}</span><strong>{recipe.views.toLocaleString("fr-FR")}</strong></div>)}</section></main></AdminShell>}
