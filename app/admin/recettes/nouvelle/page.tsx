import Link from "next/link";import { ArrowLeft } from "lucide-react";import { AdminShell } from "@/components/admin-shell";import { RecipeForm } from "@/components/recipe-form";import { createRecipe } from "@/app/admin/actions";import { requireAdmin } from "@/lib/auth";import { studioPath } from "@/lib/studio-path";import { db } from "@/lib/db";
export default async function NewRecipe(){await requireAdmin();const [categories,tags,media]=await Promise.all([db.recipeCategory.findMany({orderBy:{name:"asc"}}),db.recipeTag.findMany({orderBy:{name:"asc"}}),db.mediaAsset.findMany({orderBy:{createdAt:"desc"},take:50})]);return <AdminShell><main className="admin-main"><Link className="back" href={studioPath()}><ArrowLeft size={14}/> Retour</Link><div className="editor-head"><span className="eyebrow">NOUVELLE RECETTE</span><h1>Créer quelque chose<br/><em>de délicieux.</em></h1></div><RecipeForm action={createRecipe} categories={categories.map(x=>x.name)} tags={tags.map(x=>x.name)} media={media}/></main></AdminShell>}
