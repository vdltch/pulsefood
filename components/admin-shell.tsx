import Link from "next/link";import { BarChart3,BookOpen,LayoutDashboard,LogOut,Settings,Users } from "lucide-react";import { Logo } from "./icons";import { logoutAction } from "@/app/admin/actions";import { studioPath } from "@/lib/studio-path";
export function AdminShell({children}:{children:React.ReactNode}){const base=studioPath();return <div className="admin-shell"><aside className="admin-side"><Link href={base}><Logo/></Link><Link className="active" href={base}><LayoutDashboard size={15}/> Vue d'ensemble</Link><Link href={`${base}/recettes/nouvelle`}><BookOpen size={15}/> Nouvelle recette</Link><Link href="#"><Users size={15}/> Membres</Link><Link href="#"><BarChart3 size={15}/> Analyses</Link><Link href="#"><Settings size={15}/> Réglages</Link><form action={logoutAction}><button className="logout"><LogOut size={15}/> Déconnexion</button></form><small>PULSE STUDIO — V1.0</small></aside>{children}</div>}
