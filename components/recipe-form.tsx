import type { Recipe, Ingredient, RecipeStep } from "@prisma/client";
type FullRecipe=Recipe&{ingredients:Ingredient[];steps:RecipeStep[]};
export function RecipeForm({action,recipe}:{action:(formData:FormData)=>void|Promise<void>;recipe?:FullRecipe}){return <form action={action} className="recipe-form"><div className="form-main"><section className="form-card"><span className="eyebrow">CONTENU</span><label>Titre<input name="title" required minLength={3} defaultValue={recipe?.title}/></label><label>Description<textarea name="description" required minLength={20} rows={4} defaultValue={recipe?.description}/></label><div className="form-grid"><label>Catégorie<input name="category" required defaultValue={recipe?.category??"Bowl"}/></label><label>Difficulté<select name="difficulty" defaultValue={recipe?.difficulty??"Facile"}><option>Facile</option><option>Rapide</option><option>Intermédiaire</option><option>Weekend</option></select></label><label>Préparation (min)<input name="prepMinutes" type="number" min="1" required defaultValue={recipe?.prepMinutes??25}/></label><label>Protéines (g)<input name="protein" type="number" min="0" required defaultValue={recipe?.protein??25}/></label><label>Calories<input name="calories" type="number" min="0" required defaultValue={recipe?.calories??450}/></label></div></section><section className="form-card"><span className="eyebrow">RECETTE</span><label>Ingrédients <small>Un par ligne</small><textarea name="ingredients" required rows={9} defaultValue={recipe?.ingredients.sort((a,b)=>a.position-b.position).map(x=>x.label).join("\n")}/></label><label>Étapes <small>Une par ligne</small><textarea name="steps" required rows={9} defaultValue={recipe?.steps.sort((a,b)=>a.position-b.position).map(x=>x.body).join("\n")}/></label></section></div><aside className="form-aside"><section className="form-card"><span className="eyebrow">PUBLICATION</span><label>Statut<select name="status" defaultValue={recipe?.status??"DRAFT"}><option value="DRAFT">Brouillon</option><option value="PUBLISHED">Publié</option><option value="ARCHIVED">Archivé</option></select></label><label className="checkbox"><input name="featured" type="checkbox" defaultChecked={recipe?.featured}/> Mettre à la une</label><button className="save-button">{recipe?"Enregistrer les modifications":"Créer la recette"}</button></section><section className="form-card"><span className="eyebrow">IMAGE</span>{recipe&&<input type="hidden" name="image" value={recipe.image}/>}<label>Photo<input name="imageFile" type="file" accept="image/jpeg,image/png,image/webp" required={!recipe}/><small>JPG, PNG ou WebP — 8 Mo max. Conversion WebP automatique.</small></label></section></aside></form>}
