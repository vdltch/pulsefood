import { z } from "zod";
export const recipeInputSchema=z.object({title:z.string().trim().min(3),description:z.string().trim().min(20),image:z.string().min(1),prepMinutes:z.coerce.number().int().min(1).max(1440),protein:z.coerce.number().int().min(0).max(300),calories:z.coerce.number().int().min(0).max(5000),difficulty:z.string().min(2),category:z.string().min(2),ingredients:z.array(z.string().trim().min(1)).min(1),steps:z.array(z.string().trim().min(1)).min(1),featured:z.boolean().default(false),status:z.enum(["DRAFT","PUBLISHED","ARCHIVED"])});
export type RecipeInput=z.infer<typeof recipeInputSchema>;
